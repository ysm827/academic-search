interface:
  display_name: "Academic Search"
  short_description: "Search multidisciplinary papers and extract metadata"
  default_prompt: "Use $academic-search to search papers across disciplines, compare sources, check open-access status, and extract structured metadata."
